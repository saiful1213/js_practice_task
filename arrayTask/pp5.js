/* 
5. Combining Arrays

    i.) Create two arrays of your choice.
    ii.) Use the concat method to combine the two arrays into a new array.
    iii.) Print both the original arrays and the combined array using console.log().
*/

const num1 = [1, 23, 45, 56];
const num2 = [99, 73, 0, 6];

const newArr = num1.concat(num2);

console.log(num1, num2, newArr);