// Write a function to convert temperature from Celsius to Fahrenheit.

function celsiusToFarenheit(celsius) {
    const fahrenheit = (celsius * 1.8) + 32;
    return fahrenheit;
}

console.log(celsiusToFarenheit(36));